# Argo CD Application - demo-gitlab-argo-insecure-app (stage)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-gitlab-argo-insecure-app-stage
  namespace: argocd
  labels:
    app.kubernetes.io/name: demo-gitlab-argo-insecure-app
    environment: stage
spec:
  project: default
  source:
    repoURL: https://github.com/LongTheta/gitops-platform.git
    path: platform/apps/demo-gitlab-argo-insecure-app/overlays/stage
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: stage
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
